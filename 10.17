#include<iostream>
#include<cmath>
#include<iomanip>

int main() {
	double a, b, c, p ,s= 0;
	std::cin >> a >> b >> c;
	p = (a + b + c) / 2;
	s = sqrt(p * (p - a) * (p - b) * (p - c));
	std::cout << std::fixed << std::setprecision(1) << s << std::endl;

	return 0;

}
